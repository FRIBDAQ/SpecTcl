#
#  This file contains directory definitions that can be used
# by compilation rules.  These directories are relative to the
# symbol INSTDIR, which is assumed to be the top level 
# installation directory:
#

SPECBIN=$(INSTDIR)/bin
SPECINCS=$(INSTDIR)/include
SPECLIBS=$(INSTDIR)/lib
SPECETC=$(INSTDIR)/etc
SPECDOCS=$(INSTDIR)/doc
SPECCONTRIB=$(INSTDIR)/contrib

